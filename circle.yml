machine:
  java:
    version: oraclejdk8
  timezone:
    Asia/Tokyo
deployment:
  override:
    - mvn -s circle_settings.xml clean site deploy
  #post:
  #  - cp -r ./target/site/* $CIRCLE_TEST_REPORTS/
